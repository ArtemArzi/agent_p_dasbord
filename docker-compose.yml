version: '3.8'

# Development compose: uses bind mounts and DEBUG=true.

services:
  dashboard:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - DEBUG=true
